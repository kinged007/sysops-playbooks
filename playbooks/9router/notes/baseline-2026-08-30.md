# Baseline 2026-08-30 — models + combos order

Snapshot saved to `playbooks/9router/combos/` as starting point for next time.

- Captured `models.json:1` 90 models (ocg 24, cmc 24, openrouter 18, cf 13, ollama 6, gemini 4, combo 5) from `GET /v1/models`.
- Scores from `artificialanalysis.ai/leaderboards/models` 624, muse overridden 0.02.
- Combos: free 41 (intelligence desc), coding-pro 15 (>=50), coding-med 35 (>=40), coding-low 50 (>=30) cost asc free tie, vision 26. Order as edited in `executions/9router/combos/*.csv:1` with ranks re-sequenced.
- Generated configs: `playbooks/9router/scripts/generate-configs.py:1` -> `executions/9router/generated/` 94 models (live).

To update: edit `executions/9router/combos/*.csv` order, run `python fix-ranks.py` then copy to `playbooks/9router/combos/`.
